{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),f='../api-zod/src/generated/types/index.ts',c=fs.readFileSync(f,'utf8');fs.writeFileSync(f,c.split('\\n').filter(l=>!/(importPreviewBody|importRunBody|importHistoryResponse)/.test(l)).join('\\n'));\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.5.2"
  }
}
